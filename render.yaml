services:
  - type: web
    name: your-app-name
    env: python
    runtime: python3.11
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    build:
      packages:
        - poppler-utils
        - tesseract-ocr
